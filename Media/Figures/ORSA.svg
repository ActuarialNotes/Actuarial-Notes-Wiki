<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 46 360 366" width="360" height="366" role="img">
  <title>Two capital columns: the MCT's standard-formula requirement, and the insurer's own view — the same requirement plus the risks the formula does not capture — with the internal target drawn at the top of the second</title>
  <style>:root {
  --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c;
}
@media (prefers-color-scheme: dark) {
  :root {
    --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
    --grid: #333333; --soft: #242424; --axis: #6b6b6b;
  }
}
/* Named by the host in the embed URL (`…svg#dark`), and beats the media query
   above because a value set here lands on the drawing rather than on the root
   it inherits from. See the module docstring. */
#light:target ~ * { --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c; }
#dark:target ~ * { --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
  --grid: #333333; --soft: #242424; --axis: #6b6b6b; }
text {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto,
               "Helvetica Neue", Arial, sans-serif;
  fill: var(--ink);
  font-size: 12px;
}
.dim { fill: var(--dim); }
.sm  { font-size: 10.5px; }
.bold { font-weight: 600; }
.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 11px;
}
.card { fill: var(--surf); stroke: var(--edge); }
.axis { stroke: var(--axis); stroke-width: 1.1; fill: none; }
.grid { stroke: var(--grid); stroke-width: 1; fill: none; }
.tick { stroke: var(--axis); stroke-width: 1; }
.rule { stroke: var(--edge); stroke-width: 1; fill: none; }
.soft { fill: var(--soft); }
.curve { fill: none; stroke-width: 2; stroke-linecap: round;
         stroke-linejoin: round; }
.thin  { fill: none; stroke-width: 1.4; stroke-linecap: round; }
.dash  { stroke-dasharray: 4 3; }
.dot   { stroke-dasharray: 1.5 3; stroke-linecap: round; }</style>
  <g id="light"/><g id="dark"/>
  <g class="art">
    <rect class="card" x="0.6" y="46.6" width="358.8" height="364.8" rx="10"/>
    <rect x="62" y="176.2" width="92" height="173.8" rx="3" fill="#3b82f6" fill-opacity="0.55"/>
    <rect x="206" y="176.2" width="92" height="173.8" rx="3" fill="#3b82f6" fill-opacity="0.55"/>
    <rect x="206" y="124.2" width="92" height="52" rx="3" fill="#d97706" fill-opacity="0.14" stroke="#d97706" stroke-width="1.3" stroke-dasharray="4 3"/>
    <text x="252" y="154.2" text-anchor="middle" class="sm bold">not captured</text>
    <line x1="154" y1="176.2" x2="206" y2="176.2" class="thin dot" stroke="var(--dim)"/>
    <line x1="40" y1="106.2" x2="320" y2="106.2" class="thin" stroke="#7c3aed" stroke-width="2"/>
    <text x="40" y="98.2" text-anchor="start" class="sm bold">internal target</text>
    <line x1="40" y1="350" x2="320" y2="350" class="axis"/>
    <text x="108" y="368" text-anchor="middle" class="sm dim">MCT</text>
    <text x="252" y="368" text-anchor="middle" class="sm dim">ORSA</text>
  </g>
</svg>
