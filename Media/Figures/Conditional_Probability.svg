<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 46 360 366" width="360" height="366" role="img">
  <title>Two overlapping events inside S with everything outside B faded, so B becomes the whole space and the overlap is the share of it that is also A</title>
  <style>:root {
  --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c;
}
@media (prefers-color-scheme: dark) {
  :root {
    --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
    --grid: #333333; --soft: #242424; --axis: #6b6b6b;
  }
}
/* Named by the host in the embed URL (`…svg#dark`), and beats the media query
   above because a value set here lands on the drawing rather than on the root
   it inherits from. See the module docstring. */
#light:target ~ * { --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c; }
#dark:target ~ * { --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
  --grid: #333333; --soft: #242424; --axis: #6b6b6b; }
text {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto,
               "Helvetica Neue", Arial, sans-serif;
  fill: var(--ink);
  font-size: 12px;
}
.dim { fill: var(--dim); }
.sm  { font-size: 10.5px; }
.bold { font-weight: 600; }
.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 11px;
}
.card { fill: var(--surf); stroke: var(--edge); }
.axis { stroke: var(--axis); stroke-width: 1.1; fill: none; }
.grid { stroke: var(--grid); stroke-width: 1; fill: none; }
.tick { stroke: var(--axis); stroke-width: 1; }
.rule { stroke: var(--edge); stroke-width: 1; fill: none; }
.soft { fill: var(--soft); }
.curve { fill: none; stroke-width: 2; stroke-linecap: round;
         stroke-linejoin: round; }
.thin  { fill: none; stroke-width: 1.4; stroke-linecap: round; }
.dash  { stroke-dasharray: 4 3; }
.dot   { stroke-dasharray: 1.5 3; stroke-linecap: round; }</style>
  <g id="light"/><g id="dark"/>
  <g class="art">
    <rect class="card" x="0.6" y="46.6" width="358.8" height="364.8" rx="10"/>
    <rect x="26" y="86" width="308" height="290" rx="8" fill="var(--soft)" stroke="var(--edge)" stroke-width="1.2"/>
    <text x="39" y="102" text-anchor="middle" class="sm dim">S</text>
    <circle cx="132" cy="236" r="92" fill="#3b82f6" fill-opacity="0.2" stroke="#3b82f6" stroke-width="1.5"/>
    <circle cx="228" cy="236" r="92" fill="#d97706" fill-opacity="0.2" stroke="none"/>
    <path d="M180.0,157.51 A92,92 0 0 1 180.0,314.49 A92,92 0 0 1 180.0,157.51 Z" fill="#7c3aed" fill-opacity="0.5" stroke="none"/>
    <path d="M26,86 h308 v290 h-308 Z M136.0,236 a92,92 0 1 0 184,0 a92,92 0 1 0 -184,0 Z" fill="var(--surf)" fill-opacity="0.72" fill-rule="evenodd" stroke="none"/>
    <circle cx="228" cy="236" r="92" fill="none" stroke="#d97706" stroke-width="2.8"/>
    <text x="76" y="156" text-anchor="middle" class="bold dim">A</text>
    <text x="284" y="156" text-anchor="middle" class="bold">B</text>
    <text x="180" y="241" text-anchor="middle" class="bold">0.12</text>
    <text x="270" y="241" text-anchor="middle" class="bold">0.28</text>
  </g>
</svg>
