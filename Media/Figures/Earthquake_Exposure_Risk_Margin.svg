<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 46 360 366" width="360" height="366" role="img">
  <title>A $600M 1-in-500 earthquake PML as one column beside the resources that meet it — $372M of reinsurance, a $60M reserve and, on top, the $168M of capital the margin requires</title>
  <style>:root {
  --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c;
}
@media (prefers-color-scheme: dark) {
  :root {
    --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
    --grid: #333333; --soft: #242424; --axis: #6b6b6b;
  }
}
/* Named by the host in the embed URL (`…svg#dark`), and beats the media query
   above because a value set here lands on the drawing rather than on the root
   it inherits from. See the module docstring. */
#light:target ~ * { --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c; }
#dark:target ~ * { --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
  --grid: #333333; --soft: #242424; --axis: #6b6b6b; }
text {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto,
               "Helvetica Neue", Arial, sans-serif;
  fill: var(--ink);
  font-size: 12px;
}
.dim { fill: var(--dim); }
.sm  { font-size: 10.5px; }
.bold { font-weight: 600; }
.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 11px;
}
.card { fill: var(--surf); stroke: var(--edge); }
.axis { stroke: var(--axis); stroke-width: 1.1; fill: none; }
.grid { stroke: var(--grid); stroke-width: 1; fill: none; }
.tick { stroke: var(--axis); stroke-width: 1; }
.rule { stroke: var(--edge); stroke-width: 1; fill: none; }
.soft { fill: var(--soft); }
.curve { fill: none; stroke-width: 2; stroke-linecap: round;
         stroke-linejoin: round; }
.thin  { fill: none; stroke-width: 1.4; stroke-linecap: round; }
.dash  { stroke-dasharray: 4 3; }
.dot   { stroke-dasharray: 1.5 3; stroke-linecap: round; }</style>
  <g id="light"/><g id="dark"/>
  <g class="art">
    <rect class="card" x="0.6" y="46.6" width="358.8" height="364.8" rx="10"/>
    <rect x="60" y="100" width="88" height="252" rx="3" fill="#e11d48" fill-opacity="0.6"/>
    <text x="104" y="92" text-anchor="middle" class="sm bold">PML 600</text>
    <text x="104" y="370" text-anchor="middle" class="sm dim">1-in-500</text>
    <rect x="202" y="195.76" width="88" height="156.24" rx="3" fill="#0d9488" fill-opacity="0.6"/>
    <text x="246" y="277.88" text-anchor="middle" class="sm">reinsurance</text>
    <rect x="202" y="170.56" width="88" height="25.2" rx="3" fill="#d97706" fill-opacity="0.6"/>
    <text x="246" y="187.16" text-anchor="middle" class="sm">reserve</text>
    <rect x="202" y="100" width="88" height="70.56" rx="3" fill="#3b82f6" fill-opacity="0.6"/>
    <text x="246" y="139.28" text-anchor="middle" class="sm bold">capital 168</text>
    <text x="246" y="370" text-anchor="middle" class="sm dim">resources</text>
    <line x1="148" y1="100" x2="202" y2="100" class="thin dot" stroke="var(--dim)"/>
    <line x1="40" y1="352" x2="320" y2="352" class="axis"/>
  </g>
</svg>
