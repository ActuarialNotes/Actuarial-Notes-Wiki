---
concept: "Tree Ensemble"
exam: exam-mas-ii
topic: "C. Statistical Learning"
correct: D
---
What does a random forest do that plain bagging does not?

- A) It draws bootstrap resamples of the training rows each time
- B) It fits each new tree to the residuals left behind by the previous one
- C) It prunes every tree back using a cost-complexity penalty term
- D) It restricts each split to a random subset of the available predictors

<!-- rationale: 0: names the step both methods share, so it cannot be the difference · 1: describes boosting, a different ensemble entirely · 2: describes single-tree pruning, which ensembles deliberately skip in favour of averaging -->
