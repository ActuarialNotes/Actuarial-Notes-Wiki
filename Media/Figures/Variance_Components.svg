<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 360 470" width="360" height="470" role="img">
  <title>Total variance split into a between-group and a within-group component</title>
  <style>:root {
  --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c;
}
@media (prefers-color-scheme: dark) {
  :root {
    --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
    --grid: #333333; --soft: #242424; --axis: #6b6b6b;
  }
}
/* Named by the host in the embed URL (`…svg#dark`), and beats the media query
   above because a value set here lands on the drawing rather than on the root
   it inherits from. See the module docstring. */
#light:target ~ * { --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c; }
#dark:target ~ * { --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
  --grid: #333333; --soft: #242424; --axis: #6b6b6b; }
text {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto,
               "Helvetica Neue", Arial, sans-serif;
  fill: var(--ink);
  font-size: 12px;
}
.dim { fill: var(--dim); }
.ttl { font-size: 14px; font-weight: 600; }
.fml { font-size: 14px; font-weight: 600; }
.fml2 { font-size: 12.5px; fill: var(--dim); }
.sub { font-size: 11px; fill: var(--dim); }
.sm  { font-size: 10.5px; }
.bold { font-weight: 600; }
.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 11px;
}
.card { fill: var(--surf); stroke: var(--edge); }
.axis { stroke: var(--axis); stroke-width: 1.1; fill: none; }
.grid { stroke: var(--grid); stroke-width: 1; fill: none; }
.tick { stroke: var(--axis); stroke-width: 1; }
.rule { stroke: var(--edge); stroke-width: 1; fill: none; }
.soft { fill: var(--soft); }
.curve { fill: none; stroke-width: 2; stroke-linecap: round;
         stroke-linejoin: round; }
.thin  { fill: none; stroke-width: 1.4; stroke-linecap: round; }
.dash  { stroke-dasharray: 4 3; }
.dot   { stroke-dasharray: 1.5 3; stroke-linecap: round; }</style>
  <g id="light"/><g id="dark"/>
  <g class="art">
    <rect class="card" x="0.6" y="0.6" width="358.8" height="468.8" rx="10"/>
    <text x="180" y="40" text-anchor="middle" class="ttl">Total variance splits between and within</text>
    <line x1="28" y1="406" x2="332" y2="406" class="rule"/>
    <text x="180" y="438" text-anchor="middle" class="fml">Var(y) = σ²_b + σ²</text>
    <rect x="60" y="108" width="200" height="30" rx="4" fill="#7c3aed" fill-opacity="0.5"/>
    <rect x="60" y="138" width="200" height="120" rx="4" fill="#3b82f6" fill-opacity="0.35"/>
    <text x="160" y="127" text-anchor="middle" class="sm bold">σ²_b = 250</text>
    <text x="160" y="202" text-anchor="middle" class="sm bold">σ² = 1,000</text>
    <text x="50" y="127" text-anchor="end" class="sm dim">between</text>
    <text x="50" y="202" text-anchor="end" class="sm dim">within</text>
    <text x="180" y="286" text-anchor="middle" class="bold">total 1,250</text>
    <text x="180" y="314" text-anchor="middle" class="sm">ICC = 250 / 1,250 = 0.20</text>
    <text x="180" y="340" text-anchor="middle" class="sm dim">the mixed-model names for VHM and EPV</text>
    <text x="180" y="390" text-anchor="middle" class="sm dim">σ²_b = 0 collapses to ordinary regression</text>
  </g>
</svg>
