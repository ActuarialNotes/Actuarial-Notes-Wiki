<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 46 360 366" width="360" height="366" role="img">
  <title>Three price bars against the dashed redemption level: above it when the coupon rate exceeds the yield, level with it at par, below it when the yield is higher</title>
  <style>:root {
  --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c;
}
@media (prefers-color-scheme: dark) {
  :root {
    --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
    --grid: #333333; --soft: #242424; --axis: #6b6b6b;
  }
}
/* Named by the host in the embed URL (`…svg#dark`), and beats the media query
   above because a value set here lands on the drawing rather than on the root
   it inherits from. See the module docstring. */
#light:target ~ * { --surf: #ffffff; --edge: #c7c7c7; --ink: #000000; --dim: #424242;
  --grid: #dedede; --soft: #e8e8e8; --axis: #8c8c8c; }
#dark:target ~ * { --surf: #171717; --edge: #424242; --ink: #ffffff; --dim: #c7c7c7;
  --grid: #333333; --soft: #242424; --axis: #6b6b6b; }
text {
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto,
               "Helvetica Neue", Arial, sans-serif;
  fill: var(--ink);
  font-size: 12px;
}
.dim { fill: var(--dim); }
.sm  { font-size: 10.5px; }
.bold { font-weight: 600; }
.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: 11px;
}
.card { fill: var(--surf); stroke: var(--edge); }
.axis { stroke: var(--axis); stroke-width: 1.1; fill: none; }
.grid { stroke: var(--grid); stroke-width: 1; fill: none; }
.tick { stroke: var(--axis); stroke-width: 1; }
.rule { stroke: var(--edge); stroke-width: 1; fill: none; }
.soft { fill: var(--soft); }
.curve { fill: none; stroke-width: 2; stroke-linecap: round;
         stroke-linejoin: round; }
.thin  { fill: none; stroke-width: 1.4; stroke-linecap: round; }
.dash  { stroke-dasharray: 4 3; }
.dot   { stroke-dasharray: 1.5 3; stroke-linecap: round; }</style>
  <g id="light"/><g id="dark"/>
  <g class="art">
    <rect class="card" x="0.6" y="46.6" width="358.8" height="364.8" rx="10"/>
    <line x1="40" y1="140" x2="322" y2="140" class="thin dash" stroke="var(--dim)" stroke-width="1.2"/>
    <text x="34" y="144" text-anchor="end" class="sm bold">C</text>
    <rect x="56" y="105.8" width="64" height="224.2" rx="4" fill="#d97706" fill-opacity="0.35" stroke="#d97706" stroke-width="1.3"/>
    <text x="88" y="97.8" text-anchor="middle" class="sm bold">premium</text>
    <text x="88" y="350" text-anchor="middle" class="bold">r &gt; j</text>
    <rect x="150" y="140" width="64" height="190" rx="4" fill="#059669" fill-opacity="0.35" stroke="#059669" stroke-width="1.3"/>
    <text x="182" y="132" text-anchor="middle" class="sm bold">par</text>
    <text x="182" y="350" text-anchor="middle" class="bold">r = j</text>
    <rect x="244" y="170.4" width="64" height="159.6" rx="4" fill="#3b82f6" fill-opacity="0.35" stroke="#3b82f6" stroke-width="1.3"/>
    <text x="276" y="162.4" text-anchor="middle" class="sm bold">discount</text>
    <text x="276" y="350" text-anchor="middle" class="bold">r &lt; j</text>
  </g>
</svg>
